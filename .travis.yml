language: ruby
sudo: false

matrix:
  include:
    - rvm: 2.2.0
    - rvm: 2.3.1

branches:
  only:
    - master

before_script:
  - gem update --system
  - gem install sass --no-document
  - gem update
  - gem cleanup

script:
  - 'tests/run_tests.rb'
