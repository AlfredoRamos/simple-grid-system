language: ruby
sudo: false

matrix:
  include:
    - rvm: 2.2.0
    - rvm: 2.3.1

branches:
  only:
    - master

before_script:
  - gem install sass --no-ri --no-rdoc
  - gem update --system
  - gem cleanup
  - gem --version

script:
  - 'tests/run_tests.rb'