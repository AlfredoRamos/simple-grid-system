language: ruby

matrix:
  include:
    - rvm: 2.2
    - rvm: 2.3.1

branches:
  only:
    - master

before_install:
  - gem update --system

before_script:
  - gem update --no-document
  - gem cleanup
