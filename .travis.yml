language: ruby

matrix:
  include:
    - rvm: 2.2.6
    - rvm: 2.3.3
    - rvm: 2.4.0

branches:
  only:
    - master

before_install:
  - gem update --system

before_script:
  - gem update --no-document
  - gem cleanup
