language: ruby

matrix:
  include:
    - rvm: 2.2.7
    - rvm: 2.3.4
    - rvm: 2.4.1

branches:
  only:
    - master

before_install:
  - gem update --system

before_script:
  - gem update --no-document
  - gem cleanup
