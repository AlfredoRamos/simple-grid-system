language: ruby

matrix:
  include:
    - rvm: 2.2.8
    - rvm: 2.3.5
    - rvm: 2.4.2

branches:
  only:
    - master

cache:
  bundler: true

before_install:
  - gem update --system
  - gem update --no-document
  - gem cleanup
